[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.2",
    "psutil>=7.1.0",
    "python-nmap>=0.7.1",
    "scapy>=2.6.1",
]
